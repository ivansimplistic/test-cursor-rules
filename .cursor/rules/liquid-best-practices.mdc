---
description: 
globs: **/*.liquid
alwaysApply: false
---
- Use `capture` to store complex calculations or repeated values
- Minimize the use of `if` statements for better performance
- Leverage `assign` and `increment` for cleaner variable management
- Use `for` loops efficiently to avoid performance bottlenecks

- Keep Liquid logic simple and offload complex logic to JavaScript